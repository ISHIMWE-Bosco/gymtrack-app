name: Dev CI

on:
  pull_request:
    branches: [main, develop]

jobs:
  skip:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - run: echo "CI temporarily disabled — no codebase yet"
